
# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.mcp.json
.bg-shell/
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.next/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/

# ── Secrets — ห้าม commit เด็ดขาด ──
credentials/
*.key
*service_account*.json
*client_secret*.json
.env

# ── Browser profiles (Playwright/Chrome) ──
chrome_debug_profile/
chrome_profile/

# ── Obsidian vault config (ต่อ user เอง) ──
.obsidian/

# ── GSD migration / runtime state ──
.gsd.migrating/

# ── Runtime data + logs + downloads ──
data/bms_telemetry.db
data/token_state.json
data/token_harvest_log.ndjson
data/manual_token.txt
logs/
downloads/
data/archive/
data/processed/
data/probe/
data/test_run*/
data/raw/
data/bids/
data/company/
data/jobs_*.json
data/doc_download_*.json
data/seen_ids.json

# ── Sophia sanity-auditor throwaway probes (ไม่ commit) ──
scripts/_scratch/*
!scripts/_scratch/.gitkeep

# ── Backups (ใหญ่, ไม่ควรเข้า git) ──
backups/

# ── Misc ──
Untitled.canvas

# ── Runtime RSS + scan artifacts ──
data/rss_run_*.json
data/scan_queue_*.json
data/scan_all_depts_*_progress.json
data/gentle_scan_log.txt
data/gentle_scan_progress.json

# ── Cache / transient cursor files ──
data/road_distance_cache.json
data/_discover_alljobs_cache.json
data/cgd_projectid_cache.json
data/refresh_pending_cursor.json
data/cgd_subset.jsonl*

# ── Customer database (sensitive) ──
data/bms_customers.db
*.wal
*.shm
*.sqlite
*.sqlite3

# ── Large legacy archives ──
archives/
data/deadline_resolution_log.ndjson
data/_test_invitation*.pdf
data/session_flow_capture.ndjson
data/deadline_presence_sample.json

# work-type validation artifacts (regen ทุกครั้งที่รัน validate; เก็บ audit.md ที่ curate ไว้)
data/work_type_validation_2*.txt
data/work_type_unknown_sample.txt

data/_winner_history_156.json
data/_target_pids.json
data/_cgd_winners_raw.json
data/_cgd_winners_67_66_raw.json
data/winner_history.db
data/_wh_fetch_ckpt.json
.vercel
dashboard/parents/.vercel

# ongoing_bidder_capture runtime state (per-node, do not commit)
data/ongoing_capture_state.json
data/ongoing_capture_seen_cgd.json
data/ongoing_capture_live_tries.json
